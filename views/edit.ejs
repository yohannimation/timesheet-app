<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Modifier la ligne</title>
  <link href="/node_modules/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container">
    <h1 class="mt-5">Modifier la ligne</h1>
    
    <form action="/update-line/<%= currentYear %>/<%= index %>" method="POST" class="mt-4">
      <div class="mb-3">
        <label for="day" class="form-label">Day</label>
        <input type="date" class="form-control" id="day" name="day" value="<%= entry.day %>" required>
      </div>
      <div class="mb-3">
        <label for="start" class="form-label">Start</label>
        <input type="time" class="form-control" id="start" name="start" value="<%= entry.start %>" required>
      </div>
      <div class="mb-3">
        <label for="end" class="form-label">End</label>
        <input type="time" class="form-control" id="end" name="end" value="<%= entry.end %>" required>
      </div>
      <div class="mb-3">
        <label for="ticket" class="form-label">Ticket</label>
        <input type="text" class="form-control" id="ticket" name="ticket" value="<%= entry.ticket %>">
      </div>
      <div class="mb-3">
        <label for="state" class="form-label">State</label>
        <select class="form-select" id="state" name="state" required>
          <option value="Search" <%= entry.state === 'Search' ? 'selected' : '' %>>Search</option>
          <option value="Development" <%= entry.state === 'Development' ? 'selected' : '' %>>Development</option>
          <option value="Test" <%= entry.state === 'Test' ? 'selected' : '' %>>Test</option>
          <option value="Deployment" <%= entry.state === 'Deployment' ? 'selected' : '' %>>Deployment</option>
          <option value="Estimate" <%= entry.state === 'Estimate' ? 'selected' : '' %>>Estimate</option>
          <option value="Vacation" <%= entry.state === 'Vacation' ? 'selected' : '' %>>Vacation</option>
          <option value="School" <%= entry.state === 'School' ? 'selected' : '' %>>School</option>
          <option value="Holiday" <%= entry.state === 'Holiday' ? 'selected' : '' %>>Holiday</option>
        </select>
      </div>
      <div class="mb-3">
        <label for="project" class="form-label">Project</label>
        <input type="text" class="form-control" id="project" name="project" value="<%= entry.project %>">
      </div>
      <div class="mb-3">
        <label for="description" class="form-label">Description</label>
        <textarea class="form-control" id="description" name="description" rows="4"><%= entry.description %></textarea>
      </div>
      <div class="mb-3">
        <div class="form-check">
          <input class="form-check-input" type="checkbox" id="uploaded" name="uploaded" <%= entry.uploaded ? 'checked' : '' %>>
          <label class="form-check-label" for="uploaded">Uploaded</label>
        </div>
      </div>
      <button type="submit" class="btn btn-primary">Mettre à jour</button>
    </form>
  </div>

  <script src="/node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
